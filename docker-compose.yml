services:
  postgres:
    image: postgres:16
    container_name: campus_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: campus_lost_found
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-test-db.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ./server
    container_name: campus_server
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/campus_lost_found
      TEST_DATABASE_URL: postgresql://postgres:postgres@postgres:5432/campus_lost_found_test
      JWT_SECRET: super_secret_dev_key_change_in_prod
      NODE_ENV: development
      PORT: 3000
    ports:
      - "3000:3000"
    # Pas de volume node_modules — on utilise celui build dans l'image

volumes:
  pgdata:
